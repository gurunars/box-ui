<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menuContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.gurunars.android_utils.CircularIconButton
        android:layout_alignParentRight="true"
        android:layout_above="@+id/moveDown"
        style="@style/Button.Vertical"
        app:innerDrawable="@drawable/ic_move_up"
        android:id="@+id/moveUp" />

    <com.gurunars.android_utils.CircularIconButton
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/largeMargin"
        style="@style/Button.Vertical"
        app:innerDrawable="@drawable/ic_move_down"
        android:id="@+id/moveDown" />

    <com.gurunars.android_utils.CircularIconButton
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/selectAll"
        app:innerDrawable="@drawable/ic_delete"
        android:id="@+id/delete"
        style="@style/Button.Horizontal" />

    <com.gurunars.android_utils.CircularIconButton
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/edit"
        app:innerDrawable="@drawable/ic_select_all"
        android:id="@+id/selectAll"
        style="@style/Button.Horizontal" />

    <com.gurunars.android_utils.CircularIconButton
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/largeMargin"
        app:innerDrawable="@drawable/ic_edit"
        android:id="@+id/edit"
        style="@style/Button.Horizontal" />

</RelativeLayout>