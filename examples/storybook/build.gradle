ext.appId="com.gurunars.storybook.example"

apply from: '../example.gradle'

dependencies {
    implementation project(':storybook')
    api project(':storybook-annotations')
    kapt project(':storybook-registry')
}
