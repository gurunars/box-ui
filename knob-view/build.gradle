apply from: '../all-modules.gradle'

description = """
A component to conditionally show only one of the views from a
given selection.
"""

dependencies {
    compile "org.jetbrains.anko:anko:$anko_version"
    compile project(':android-utils')
    compile project(':databinding')
}

dokka {}
